import React from 'react';


const buttonStyles = {
  border: '2px solid #eee',
  borderRadius: 42,
  backgroundColor: 'white',
  cursor: 'pointer',
  fontSize: 22,
  width:42,
  height:42,
  padding: '4px 0px',
  margin: 10,
};

const Button = ({ children, onClick }) => (
  <button
    style={buttonStyles}
    onClick={onClick}
  >
    <img src="https://s3-us-west-1.amazonaws.com/jogabocitypages/waitlist-s2.png"/>
  </button>
);

Button.propTypes = {
  onClick: React.PropTypes.func
};

export default Button;
